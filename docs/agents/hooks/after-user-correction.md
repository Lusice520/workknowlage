# Hook: After User Correction

当 Lusice 纠正 WorkKnowlage-specific assumption 时：

1. 承认修正。
2. 立即按修正行动。
3. 如果这是 durable project knowledge，默认写入 `docs/agents/memory/project-lessons.md`。

Global lessons 记录协作偏好；project lessons 记录 WorkKnowlage-specific facts。
